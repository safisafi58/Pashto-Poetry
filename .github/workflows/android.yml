name: Build Android APKs

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build Both APKs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Decode Debug Keystore
        run: |
          if [ -f "debug.keystore.base64" ]; then
            python3 -c "import base64; open('debug.keystore', 'wb').write(base64.b64decode(open('debug.keystore.base64').read().strip()))"
          fi

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build User App Debug APK
        run: ./gradlew :app:assembleDebug

      - name: Upload User App Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: Pashto-Poetry-User-App.apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Build Admin App Debug APK
        run: ./gradlew :admin:assembleDebug

      - name: Upload Admin App Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: Pashto-Poetry-Admin-App.apk
          path: admin/build/outputs/apk/debug/admin-debug.apk
          if-no-files-found: error
